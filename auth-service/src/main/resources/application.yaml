spring:
  application:
    name: auth-service
  ## Database Configuration ##
  datasource:
    url: jdbc:oracle:thin:@//localhost:1522/xepdb1
    username: system
    password: password
    driver-class-name: oracle.jdbc.driver.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.OracleDialect
    properties:
      hibernate:
        format_sql: true
  ## Email Configuration ##
  mail:
    host: smtp.gmail.com
    port: 587
    username: noreply.epurchase@gmail.com
    password: ${MAIL_PSW}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

twilio:
  account-sid: ${TWILIO_ACCOUNT_SID}
  auth-token: ${TWILIO_AUTH_TOKEN}
  phone-number: +13502204223

server:
  port: 8081
  servlet:
    context-path: /authentication

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, loggers

jwt:
  secret: w7qQkV0cQ7hYk2N9Jq3sVv8bF5xKz1LrP4dY2mN6TgH9cW0e
  ## 30 minutes ##
  expiration: 1800000